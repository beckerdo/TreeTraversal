package info.danbecker.tree;

import info.danbecker.tree.TreeTraversal;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.junit.Test;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertTrue;

public class TreeTraversalTest {
	@SuppressWarnings("unused")
	private static Logger logger = LoggerFactory.getLogger(TreeTraversalTest.class);
	
	@Test
    public void testTreeTraversal() {
		assertTrue( "cleaned copy directory exact size", 0 == 0 );
	}
	
}